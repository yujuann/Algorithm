const solution = (num1, num2) => (num1 / num2) << 0;

// ; 나누기 연산자로 /기를 사용하면 소수점을 없애기 위해  부동 소수점 연산자 <<0
// ; <<0 를 0 만큼 이동시킨다

// 다른 사람 풀이 참고
// function solution (num1, num2) {
//     return parseInt (num1 / num2);
// }

// const solution = (num1, num2) => Math.floor(num1 / num2)
