
function solution(numbers) {
  return numbers.map((a) => a * 2);
}
// numbers의 각 원소에 두배한 원소를 가진 배열을 리턴하도록
// solution함수에 완성시켰습니다.

// const solution = numbers => numbers.map(a => a*2)
const solution = (numbers) => numbers.map((i) => i *2)

한줄로 만든 배열 2배로 만들기 